{
	"tasks": [
		{
			"label": "Run Debug",
			"type": "shell",
			"command": "make debug -C ${workspaceFolder}",
			"group": "build",
			"presentation": {
				"reveal": "always",
				"panel": "new",
				"close": true
			}
		},
		{
			"label": "Run Tests",
			"type": "shell",
			"command": "make tests -C ${workspaceFolder}",
			"group": "build",
			"presentation": {
				"reveal": "always",
				"panel": "new",
				"close": true
			}
		},

	],
	"version": "2.0.0"
}
